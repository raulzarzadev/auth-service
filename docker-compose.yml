version: "3.9"

services:
  signup:
    build: .
    ports:
    - "3015:3015"
    environment: 
      - EMAIL_USER
      - EMAIL_PASSWORD
      - SIGNUP_PORT
      - JWT_SECRET_TEXT
      - FRONT_HOST
      - USERS_SERVICE_DB
    volumes: 
      - .:/usr/src/app
